<UserControl x:Class="budiga_app.MVVM.View.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:budiga_app.MVVM.View"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"                          
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1000">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10,10,10,10">
                <Button Grid.Row="3" Content="Add" HorizontalAlignment="Center" VerticalAlignment="Top" Width="120" BorderBrush="{x:Null}" Background="#FF355ACC" FontStretch="Normal" FontStyle="Normal">
                    <Button.Foreground>
                        <SolidColorBrush Color="White" Opacity="0.87"/>
                    </Button.Foreground>
                </Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10,10,10,10" Width="969">
                <materialDesign:Card Margin="0 0 10 0" VerticalAlignment="Top" Height="314" Width="573">
                    <DataGrid d:ItemsSource="{d:SampleData ItemCount=20}"/>
                </materialDesign:Card>
                <materialDesign:Card HorizontalAlignment="Right" Margin="0 0 10 0" VerticalAlignment="Top" Height="313" Width="370">
                    <DataGrid d:ItemsSource="{d:SampleData ItemCount=5}"/>
                </materialDesign:Card>
            </StackPanel>
        </StackPanel>        
    </Grid>
</UserControl>
