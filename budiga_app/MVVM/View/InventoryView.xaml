<UserControl x:Class="budiga_app.MVVM.View.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:budiga_app.MVVM.View"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"                          
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1000">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10">
                <materialDesign:Card HorizontalAlignment="Right" Margin="0 0 10 0" VerticalAlignment="Top" Height="314" Width="550">
                    <DataGrid d:ItemsSource="{d:SampleData ItemCount=20}"/>
                </materialDesign:Card>
                <materialDesign:Card HorizontalAlignment="Right" Margin="0 0 10 0" VerticalAlignment="Top" Height="313" Width="370">
                    <DataGrid d:ItemsSource="{d:SampleData ItemCount=5}"/>
                </materialDesign:Card>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10">
                <materialDesign:Card HorizontalAlignment="Right" Margin="0 0 10 0" VerticalAlignment="Top" Height="270" Width="938">
                    <DataGrid d:ItemsSource="{d:SampleData ItemCount=15}"/>
                </materialDesign:Card>
            </StackPanel>
        </StackPanel>        
    </Grid>
</UserControl>
